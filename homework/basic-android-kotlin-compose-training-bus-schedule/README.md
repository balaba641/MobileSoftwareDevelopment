# Bus Schedule

This is the Lab14 project.

Open this directory in Android Studio and complete the Room database tasks described in `../Lab14.md`.
The project currently displays example data from `BusScheduleViewModel`; your work is to replace that
example data with records loaded from `app/src/main/assets/database/bus_schedule.db`.
